<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sole Traders</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.4/css/bulma.min.css">
</head>
<body>

    <nav>
        <ul>
            <li><strong><a href="/">Sole Traders</a></strong></li>
            <li><a href="/directory">Directory</a></li>
            
            <% if (!user.isloggedin) { %>
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>
            <% } else { %>
                <% if (user.role === 'Trader') { %>
                    <li><a href="/view-profile">View Profile</a></li>
                    <li><a href="/view-bookings">View Bookings</a></li>
                    <li><a href="/logout">Logout</a></li>
                <% } else if (user.role === 'Client') { %>
                    <!-- <li><a href="/view-bookings">Bookings</a></li> got to decide what to do with customer bookings and how to view-->
                    <li><a href="/logout">Logout</a></li>
                <% } else if (user.role === 'Admin') { %>
                    <li><a href="/logout">Logout</a></li>
                <% } %>
            <% } %>
        </ul>
    </nav>
    <hr>

<main>